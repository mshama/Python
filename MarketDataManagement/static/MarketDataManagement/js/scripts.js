/**
 * 
 */

function goldenrecord_overlayClick() {
	el = document.getElementById("goldenrecordfield-overlay");
	el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
}

function datasource_overlayClick() {
	el = document.getElementById("datasourcefield-overlay");
	el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
}

function fieldmapping_overlayClick() {
	el = document.getElementById("fieldMapping-overlay");
	el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
}

